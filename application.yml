spring:
  application:
    name: Config-Default
eureka:
  instance:
    ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defualtzone: http://localhost:8761/eureka/
